server:
  port: ${PORT:8080}

spring:
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: none

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}
  upload:
    dir: ${APP_UPLOAD_DIR:./uploads}
